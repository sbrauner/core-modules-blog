<%
processArgs("search");
var perma = "";
log(tojson(search.args[0]));
perma = search.args.map(function(obj){ return obj.key + "=" + obj.value; }).join("&");
log(perma);
%>
<option value="<%= perma %>" id="Search_<%= search._id %>_name"><%= search.name %></option>
