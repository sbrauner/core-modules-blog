<%
processArgs("search");
var perma = "";
log(tojson(search.args[0]));
for(x in search.args) {
   perma += "&"+search.args[x].key+"="+search.args[x].value
}
log(perma);
%>
<option value="<%= perma %>"><%= search.name %></option>
