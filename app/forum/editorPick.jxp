<%
core.app.forum.data.thread();

thread = db.forum.threads.findOne( { _id : request.thread } );

if(thread.editorPick) {
   thread.editorPick = false;
}
else {
   thread.editorPick = true;
}

thread.save();

%>
