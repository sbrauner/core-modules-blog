<%
core.forum.topic();
var fid = request.fid;
var f = db.forum.forums.findOne({_id: fid});
var list = Forum.Topic.list(f);
if (list)
    list.forEach(Forum.renderer.topic);
else
    print ("No forums right now!");
%>
<a href="makepost?f_id=<%= fid %>">New topic</a>
