<%
core.forum.thread();
    var t = arguments[0];
var f = t.findFirstPost();
if(f == null) f = {title: "(unknown)", author: "(unknown)" };
var l = t.latestPost;
%>
<div>
<a href="viewthread?id=<%= t._id%>"><%= f.title%></a> started by <%= f.author %>
(<%= t.count.toFixed(0) %> posts so far)
Started at <%= f.ts %>. <br/>
<a href="viewthread?id=<%= t._id %>#<%= l._id %>">Most recent post</a> at <%= l.ts %>

</div>
