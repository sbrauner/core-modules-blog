<%
if ( jxp.pieces && jxp.pieces.header ){ jxp.pieces.header(); }

core.forum.topic();
var topic = db.forum.topics.findOne({_id: request.tid});
print("<!-- " + topic._id + "-->");


Forum.renderer.post(topic.findFirstPost());

if ( jxp.pieces && jxp.pieces.footer ){ jxp.pieces.footer(); }
%>
