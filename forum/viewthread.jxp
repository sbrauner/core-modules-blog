<%
if ( jxp.pieces && jxp.pieces.header ){ jxp.pieces.header(); }
else print("<link rel=\"stylesheet\" type=\"text/css\" href=\"css/forum.css\"/>");
%>

<%
core.forum.thread();
var thread = db.forum.threads.findOne({_id: request.tid});
print("<!-- " + thread._id + "-->");


Forum.renderer.post(thread.findFirstPost());
%>

<%
if ( jxp.pieces && jxp.pieces.footer ){ jxp.pieces.footer(); }
%>
